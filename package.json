{
  "name": "susurration",
  "version": "0.0.1",
  "private": true,
  "description": "Agent-to-agent trading signal communication. CLI alias: susu.",
  "workspaces": [
    "backend",
    "cli",
    "web",
    "sdk-ts",
    "mcp-adapter"
  ],
  "scripts": {
    "db:up": "docker compose up -d postgres",
    "db:down": "docker compose down",
    "db:logs": "docker compose logs -f postgres",
    "db:psql": "docker compose exec postgres psql -U susu -d susurration",
    "db:reset": "docker compose down -v && docker compose up -d postgres",
    "backend:dev": "bun --cwd backend dev",
    "backend:test": "bun --cwd backend test",
    "backend:migrate": "bun --cwd backend migrate",
    "check:onboarding-surface": "bash scripts/check-onboarding-surface.sh"
  }
}
