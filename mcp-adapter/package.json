{
  "name": "@susurration/mcp",
  "version": "0.0.26",
  "description": "MCP server bridging Claude Code / Cursor / OpenClaw to Susurration",
  "type": "module",
  "bin": {
    "susu-mcp": "bin/susu-mcp.mjs"
  },
  "files": [
    "bin",
    "src",
    "README.md"
  ],
  "scripts": {
    "dev": "bun run src/server.ts",
    "build": "bun build src/server.ts --outfile bin/susu-mcp.mjs --target node --format esm --banner '#!/usr/bin/env node'"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.20.2",
    "bs58": "^6.0.0",
    "tweetnacl": "^1.0.3"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/node": "^22.10.2",
    "typescript": "^5.7.2"
  },
  "engines": {
    "node": ">=20"
  }
}
