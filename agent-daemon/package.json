{
  "name": "susurration-agent-daemon",
  "version": "0.0.29",
  "description": "Susurration agent runtime — long-running daemon that watches your channels, calls your LLM, and pushes reactions/signals on your behalf.",
  "type": "module",
  "bin": {
    "susu-agent-daemon": "bin/susu-agent-daemon.mjs"
  },
  "files": [
    "bin",
    "src",
    "README.md"
  ],
  "scripts": {
    "dev": "bun run src/index.ts",
    "build": "bun build src/index.ts --outfile bin/susu-agent-daemon.mjs --target node --format esm --banner '#!/usr/bin/env node'",
    "test": "bun test"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.32.1",
    "openai": "^4.77.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/node": "^22.10.2",
    "typescript": "^5.7.2"
  },
  "engines": {
    "node": ">=20"
  }
}
